"""Given a function changeCase(s), the task is to convert the strings from upper case to lower case.

Input : A string in upper case

Output : Change case of the string in lower case
"""
def changecase(str):
    convert2upper = str.upper(slower)
    convert2lower = str.lower(supper)
    return(convert2upper, convert2lower)

slower = input("Enter a string in lower case: \n")
supper = input("Enter a string in upper case: \n")
print("String converted in uppercase is: \n" ,changecase(str))
